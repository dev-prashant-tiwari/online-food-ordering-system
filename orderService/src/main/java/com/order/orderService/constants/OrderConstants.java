package com.order.orderService.constants;

public class OrderConstants {
    public enum OrderStatus {
        Placed,Ready,Delivered,Accepted
    }

    public enum PaymentStatus{
        Pending,Paid
    }
}
